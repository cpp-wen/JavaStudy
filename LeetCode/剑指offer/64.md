```java
class Solution {
    public int sumNums(int n) {
        //有点偏向数学题范围 用处感觉不是很大
        //n && (n+=sumNums(n-1)); c++ 的写法 java会报错
        int sum=n;
        boolean b=(n>0)&&(sum+=sumNums(n-1))>0;
        return sum;
    }
}
```

