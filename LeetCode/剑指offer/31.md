```java
class Solution {
    public boolean validateStackSequences(int[] pushed, int[] popped) {
        //感觉是一个暴力的题目 stack peek如果不和popped第一个相同就一直push 
        //如果最后没有可以pushed 元素而且popped 不为空就 false 否则 ture
        Stack<Integer> stack=new Stack<>();
        int j=0;
        for( int elem:pushed){
            stack.push(elem);
            //这个while是关键
            while(j<popped.length&&!stack.isEmpty()&&stack.peek()==popped[j]){
                stack.pop();
                j++;
            }
        }
        return j==popped.length;
    }
}
```

